<nav class="bg-gray-900 shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <a href="/" class="text-2xl font-bold text-white">Blogify</a>
      </div>

      <!-- Navigation Links -->
      <div class="flex space-x-6 items-center">
        <a href="/" class="text-gray-300 hover:text-white font-medium">Home</a>
        <% if (locals.user) { %>
        <a href="/blog/add-new" class="text-gray-300 hover:text-white font-medium"
          >Add Blog</a
        >

        <!-- Logged-in User Badge -->
        <span
          class="bg-gray-700 text-white px-3 py-1 rounded-full text-sm font-semibold"
        >
          🧑‍💻 <%= user.fullName %>
        </span>

        <form action="/user/logout" method="POST">
          <button
            class="bg-red-600 hover:bg-red-700 text-white px-4 py-1 rounded-md"
          >
            Logout
          </button>
        </form>
        <% } else {%>
        <a
          href="/user/signup"
          class="bg-green-600 hover:bg-green-700 text-white px-4 py-1 rounded-md font-medium transition duration-200"
          >Register</a
        >
        <a
          href="/user/signin"
          class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-1 rounded-md font-medium transition duration-200"
          >Signin</a
        >

        <% } %>
      </div>
    </div>
  </div>
</nav>
